# nam_smartinit configuration
# Jeff McQueen 01/2013 

# export ymd=20130313   

# ffhr,cyc,RUNTYP set in RUN_ALL
#TEST export ffhr=03        # end of 3 hr forecast processing block
#TEST export cyc=06

# OUTPUT REGION TO DOWNSCALE TO
# valid RUNTYP=conus,ak,hi,pr,conusnest2p5,aknest3,ak_rtmages,gm
#TEST export RUNTYP=ak    

export RUN_ENVIR=dev
export envir=dev

#####################################################################################
# This block is for Developer's test run:
# Run config file to get input parameters
# This config file should define the following variables
# DATA_IN: Location of working directory, default to /tmpnwprd
# SENDECF: If the job is to be running using SMS, default to YES
# SENDDBN: Set to NO for developers, default to YES
# COM_IN:  Directory for input files, default to /com/$NET/${envir}
# COM_OUT: Directory for output file, default to /com/$NET/${envir}
# gespath: Directory for the guess or restart files, default to /nwges/${envir}

#  USED BY RUN_ALL JOB to set up Development input and output file directies
#  Added NET to define COM_IN for guam /com/hiresw/prod
#####################################################################################
export SENDECF=NO
export SENDDBN=NO
export SENDDBN_GB2=NO

export HOMEnam=/meso/save/$USER/smart-dev    # location of exec,scripts,ush dirs
export UTILparm=/meso/noscrub/$USER/nwprod/util/parm #grib2 parm files
#RETROS export COMROOT=/meso/noscrub/$USER/com
export COMROOT=/com
#
export COM_IN=${COMROOT}/${NET}/prod
export COM_SREF=${COMROOT}/sref/prod
export COM_OUT=/ptmp/${USER}/smart${RUNTYP}
if [ $RUNTYP = ak_rtmages ];then export COM_OUT=/ptmp/${USER}/smartak;fi

export jlogfile=${COM_OUT}/jlogfile
export pcom=$COM_OUT
